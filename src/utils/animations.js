// Import all animations from the animations directory
import { animationRegistry, animationConfig } from './animations/index';

// Export the animation registry and config
export { animationRegistry, animationConfig };

